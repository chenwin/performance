1、使用工具前务必认真阅读README.txt和config.conf

2、config.conf务必按照要求的格式进行配置，否则工具无法正常运行。

3、网络测试需要准备两台虚拟机，而且该工具部署到网络测试时client端所在的虚拟机上；

4、存储测试时，需要按照要求给部署该工具的虚拟机挂载数据盘进行测试。

配置完config.conf后，执行
sh install.sh 
sh run.sh 